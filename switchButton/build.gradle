apply plugin: 'com.android.library'

android {
    compileSdkVersion 34
    buildToolsVersion '30.0.2'

    defaultConfig {
        minSdkVersion 19
        targetSdkVersion 34
        versionCode 3
        versionName "0.0.3"
    }

    lintOptions {
        abortOnError false
    }
}